# v8.2.6 ポータル→TIME直接編集修正版

- `?emp=EMP-004&date=YYYY-MM-DD&edit=1` を受けたら対象社員へ切替
- 対象日を含む月度へ自動移動
- 日別編集ダイアログを直接開く
- 初期化やFirebase同期のタイミングで失敗した場合は最大12回再試行
- `dialog.showModal()` 非対応時は `open` 属性でフォールバック
- 最終的に開けない場合は「編集」タブへ移動して案内

既存勤怠・パスワード・EMPデータは変更しません。
