# v8.2.7 ポータル→TIME インライン編集版

## 変更
これまで:
ポータル → TIME月間画面 → dayEditDialogを自動表示

今回:
ポータル → EMP切替 → 対象締め月へ移動 → 対象日の月間行を自動展開 → その場で編集

TIME v8.1/v8.2が元々持っている `openOverviewInlineEdit()` を使うため、
別ダイアログを自動表示する方式より安定します。

## 操作
社内業務ポータル
→ 勤怠
→ 対象社員「TIMEで編集」
→ TIMEの該当日が自動的に展開
→ 勤務区分、出勤、退勤、外出、戻り、備考を編集
→ 保存

保存内容はEMP別TIMEへ入り、共通ストア経由でポータルにも反映されます。

## 反映
timeリポジトリへZIP展開内容をそのまま上書き。
反映後はCtrl+Shift+Rまたはスマホでページを開き直してください。
