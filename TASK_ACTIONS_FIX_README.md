# v8.2 / portal v1.1.1 タスク操作修正版

## 修正内容
タスク編集モーダルの下部に、既存タスクの場合は必ず次の3ボタンを表示します。

- 日延べ
- ペンディング
- 削除

スマホでは保存/キャンセルと同じ固定フッター内に表示されます。

さらに日フォーカス下部のタスク一覧にも「削除」ボタンを追加しました。
モーダルが万一表示崩れしても一覧から削除できます。

## 削除の考え方
削除は「誤入力・重複登録・実際には存在しなかったタスク」向けです。
予定変更には日延べ、保留にはペンディングを使うことを推奨します。

## GitHub
このZIPは time 側に上書きしてください。
portal/ フォルダも含めて、展開した中身を time リポジトリのルートへアップロードします。
