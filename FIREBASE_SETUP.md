# 勤怠管理 v7.0 Firebase設定

## 1. メール／パスワード認証を有効化

Firebase Console → Authentication → ログイン方法

「メール／パスワード」を有効にします。

## 2. ログイン用ユーザーを作成

Authentication → ユーザー → ユーザーを追加

任意のメールアドレスと十分に長いパスワードを設定します。
PCとスマホでは同じメールアドレスとパスワードを使用します。

アプリには利用者登録機能を入れていません。
第三者が自由にアカウントを作れないようにするためです。

## 3. Firestoreルール

Firestore Database → ルール

ZIP内の `firestore.rules` の内容へ置き換え、「公開」を押します。

共有台帳の保存先は次です。

```text
shared/attendance-main
```

ログインしていない端末からは読み書きできません。

## 4. GitHubへアップロード

ZIP内の全ファイルをリポジトリ直下へ上書きし、コミットします。

## 5. 既存データを移行

1. 既存データが入っているPCでv7.0を開く
2. メールアドレスとパスワードでログイン
3. 「この端末からクラウドへ送る」を押す
4. スマホで同じアカウントへログイン
5. 「クラウドから再読込」を押す

以後は、スマホとPCの変更が自動同期されます。
