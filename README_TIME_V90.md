# TIME v9.0 一方向スナップショット版

TIMEは勤怠の編集元です。画面構成・URLをポータルに合わせて変更しません。

保存時に `hokuyou.time.snapshot.v1` へ以下を書き出します。
- EMP別勤怠実績
- 出退勤、外出、戻り、備考
- TIMEの暦（法定休日・所定休日等）
- EMP別設定

ポータルはこのキーを読むだけです。

## 反映先
既存の `time` リポジトリへ、このZIPの中身を上書きしてください。
